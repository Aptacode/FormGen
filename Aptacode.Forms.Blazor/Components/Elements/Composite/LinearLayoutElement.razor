@inherits LinearLayoutElementBase

@foreach (var child in ViewModel.Children)
{
    if (ViewModel.LayoutOrientation == Aptacode.Forms.Shared.Enums.LayoutOrientation.Vertical)
    {                    
        <Row>
            <FormElement ViewModel="@child"/>
        </Row>
    }
    else
    {
        <Column>
            <FormElement ViewModel="@child"/>
        </Column>
    }

}