<UserControl x:Class="Aptacode.Forms.Wpf.Views.Elements.Controls.Fields.CheckElementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel LastChildFill="True">
        <TextBlock Text="{Binding Label.Text}" DockPanel.Dock="Left" Width="150" TextWrapping="WrapWithOverflow"
                   TextAlignment="Right" VerticalAlignment="Center" Margin="8" />
        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay, ValidatesOnDataErrors=True}" Content="{Binding Content}" />
    </DockPanel>
</UserControl>