<UserControl x:Class="Aptacode.Forms.Wpf.Views.Layout.FormGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:layout="clr-namespace:Aptacode.Forms.Shared.ViewModels.Layout;assembly=Aptacode.Forms.Shared"
             xmlns:layout1="clr-namespace:Aptacode.Forms.Wpf.Views.Layout"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <GroupBox Header="{Binding Label}">
        <ItemsControl ItemsSource="{Binding Rows}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type layout:FormRowViewModel}">
                    <layout1:FormRowView DataContext="{Binding}" Margin="16" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </GroupBox>

</UserControl>